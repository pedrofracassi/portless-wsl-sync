{
  "name": "portless-sync",
  "version": "1.0.0",
  "description": "Syncs the portless local CA certificate from WSL to the Windows host trust store",
  "type": "module",
  "bin": {
    "portless-sync": "./bin/portless-sync.js"
  },
  "scripts": {
    "start": "node bin/portless-sync.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
